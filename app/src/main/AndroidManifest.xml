<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chrisfry.socialq">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".userinterface.App"
        android:allowBackup="true"
        android:icon="@mipmap/app_launcher_icon"
        android:roundIcon="@mipmap/app_launcher_icon_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/IceTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- START APPLICATION ACTIVITIES -->
        <!-- Base activity for fragment based application management -->
        <activity
            android:name="com.chrisfry.socialq.userinterface.activities.BaseSpotifyActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
        </activity>

        <!-- Activity for Launch Screen -->
        <activity
            android:name="com.chrisfry.socialq.userinterface.activities.StartActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:launchMode="singleTop">
        </activity>

        <activity android:name=".userinterface.activities.LaunchActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity for Host Screen -->
        <activity
            android:name="com.chrisfry.socialq.userinterface.activities.HostActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- Activity for Client Screen -->
        <activity
            android:name="com.chrisfry.socialq.userinterface.activities.ClientActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- Activity for Queue Connect Screen (Bluetooth) -->

        <!-- Activity for Queue Connect Screen (Nearby Devices) KOTLIN -->
        <activity
            android:name="com.chrisfry.socialq.userinterface.activities.QueueConnectActivityNearbyDevices"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />

        <!-- Activity for Search Screen -->
        <activity
            android:name="com.chrisfry.socialq.userinterface.activities.SearchActivity"
            android:label="@string/search_activity_name"
            android:screenOrientation="portrait"/>

        <!-- Activity for returning Spotify login requests to service -->
        <activity android:name=".userinterface.activities.AccessTokenReceiverActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"/>

        <!-- Activity for Spotify login (created by Spotify) -->
        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <!-- END APPLICATION ACTIVITIES -->

        <!-- START APPLICATION SERVICES -->
        <!-- Service for playing queue in background -->
        <service
            android:name="com.chrisfry.socialq.services.PlayQueueService"
            android:description="@string/play_queue_service_description"
            android:exported="false" />

        <!-- Service For Host -->
        <service android:name=".services.HostService"
            android:description="@string/host_service_description"
            android:exported="false" />

        <!-- Service For Client -->
        <service android:name=".services.ClientService"
            android:description="@string/client_service_description"
            android:exported="false" />
        <!-- END APPLICATION SERVICES -->
    </application>

</manifest>
